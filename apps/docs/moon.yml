language: "typescript"
type: "library"
platform: "bun"

tasks:
  build:
    command: "nuxt build"
    inputs:
      - "src/**/*"
    outputs:
      - ".output/**"

  dev:
    command: "nuxt dev"
    inputs:
      - "src/**/*"
    local: true
