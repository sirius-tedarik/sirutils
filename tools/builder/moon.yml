language: "typescript"
type: "library"
platform: "bun"

tasks:
  build:
    command: "bun run src/index.ts build src/index.ts -a"
    deps:
      - schema:build
    inputs:
      - "src/**/*"
    outputs:
      - "dist/**"
