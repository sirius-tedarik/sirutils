language: "typescript"
type: "library"
platform: "bun"

tasks:
  build:
    command: "bun x sirbuilder build -a"
    deps:
      - "builder:build"
      - "toolbox:build"
      - "schema:build"
    inputs:
      - "src/**/*"
    outputs:
      - "dist/**"
