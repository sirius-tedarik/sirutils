language: "typescript"
type: "library"
platform: "bun"

tasks:
  build:
    command: "bun x sircli build -a"
    deps:
      - "sircli:build"
    inputs:
      - "src/**/*"
    outputs:
      - "dist/**"
