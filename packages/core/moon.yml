language: "typescript"
type: "library"
platform: "bun"

tasks:
  build:
    command: "bun x builder build src/index.ts -a"
    deps:
      - "builder:build"
    inputs:
      - "src/**/*"
    outputs:
      - "dist/**"
  watch:
    command: "bun x builder build src/index.ts -a -w"
    deps:
      - "builder:build"
    local: true
    inputs:
      - "src/**/*"
